# FinanceTrackerPro - SonarQube Configuration

# Server (local instance)
sonar.host.url=http://localhost:9000

# Project identification
sonar.projectKey=financetrackerpro
sonar.projectName=FinanceTrackerPro
sonar.projectVersion=1.0

# Source code directory
sonar.sources=src
sonar.sourceEncoding=UTF-8

# Exclusions
sonar.exclusions=**/*.test.ts,**/*.test.tsx,**/*.spec.ts,**/*.spec.tsx,**/node_modules/**,**/.next/**,**/dist/**,**/build/**,**/coverage/**,**/prisma/migrations/**,src/app/globals.css

# Test files
sonar.tests=src
sonar.test.inclusions=**/*.test.ts,**/*.test.tsx,**/*.spec.ts,**/*.spec.tsx

# Coverage
sonar.javascript.lcov.reportPaths=coverage/lcov.info
sonar.typescript.lcov.reportPaths=coverage/lcov.info

# TypeScript settings
sonar.typescript.tsconfigPath=tsconfig.json

# Code quality thresholds
sonar.qualitygate.wait=true
sonar.coverage.exclusions=**/*.test.ts,**/*.test.tsx,**/*.config.ts,**/*.config.js,**/types/**,src/app/**,src/lib/db-check.ts,src/lib/i18n-cookies.ts,src/actions/language.actions.ts,src/store/ui.store.ts,src/lib/db/index.ts,src/config/navigation.ts,src/lib/errors/api-errors.ts,src/lib/validations/auth.ts
# Duplication
sonar.cpd.exclusions=**/*.test.ts,**/*.test.tsx
